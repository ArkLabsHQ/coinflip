# Dependencies
node_modules/
/dist

# Local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.vscode

# Nixpacks
.nixpacks

# Docker files
dist/
node_modules/
.DS_Store

# Superpowers docs
docs/superpowers
docs/superpowers/**

# Working notes / plans — not part of the project history
docs/plans

# Temp data dirs created by e2e tests (incl. the load test's DATA_DIR=./data-load)
packages/e2e/data
packages/e2e/data-*

# Local deploy compose (carries admin auth secret) — never commit
docker-compose.mutinynet.yml

# Test/dev artifacts
.playwright-mcp/
*.tgz
# …except vendored SDK tarballs we depend on (file: path in package.json).
!/vendor/*.tgz

.research/

# Trust-model audit — local-only research doc, not part of the shipped repo.
docs/TRUSTLESS-AUDIT.md
